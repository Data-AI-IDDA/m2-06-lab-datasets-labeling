## Dataset metadata
- Student ID: 3
- Rank: 3
- Class size (N): 33


**Number of images downloaded and labeled successfully:**

At first, I selected 120 image IDs to have extra images in case some downloads failed. During the download process, 13 images failed, so 107 images were downloaded successfully. After generating the label files and removing the failed image IDs, I selected 100 image-label pairs for the final dataset, so the submitted dataset contains 100 correctly matched images with corresponding YOLO labels.

**Total time spent (rough estimate):**

The whole process took about 45-50 minutes. This included preparing the metadata files, generating the master list of image IDs, selecting my assigned images, downloading them, generating YOLO labels, and running the verification script to check the dataset.

